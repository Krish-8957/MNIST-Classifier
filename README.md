# MNIST Handwritten Digit Classifier (Training Project)

This is a training project that I built while learning PyTorch and Deep Learning. The objective of this project is to classify handwritten digits (0–9) from the MNIST dataset using a neural network.

The purpose of this project was to gain hands-on experience with fundamental deep learning concepts such as tensors, neural network layers, activation functions, loss functions, backpropagation, optimizers, and model training.

The model takes a 28×28 grayscale image as input and predicts the digit present in the image. Matplotlib is used to visualize sample images and predictions

## Technologies Used

* Python
* PyTorch
* Torchvision
* Matplotlib

## Concepts Practiced

* Tensors and Autograd
* Neural Networks
* Forward Propagation
* Backpropagation
* ReLU Activation Function
* Cross Entropy Loss
* Adam Optimizer
* DataLoader
* Model Training and Evaluation

## Dataset

The project uses the MNIST dataset, which contains handwritten digit images for training and testing machine learning models.

## Purpose

This project was created for learning and practicing Deep Learning with PyTorch. It serves as a foundation for more advanced projects such as CNNs, Computer Vision applications, and Image Classification systems.

## Future Learning Goals

* Build a CNN-based digit classifier
* Train models on custom image datasets
* Learn Transfer Learning
* Deploy Deep Learning models

